<div class="title rounded-outline">
    <h2>All Staff</h2>
    <h4 class="text-muted">Pick someone to view details.</h4>
</div>

<table class="table table-sm">
    <thead class="thead-dark">
        <tr>
            <th scope="col">Category</th>
            <th scope="col">Name</th>
            <th scope="col" class="text-center">Profile</th>
        </tr>
    </thead>
    <tbody>

        {{#each groups as |group groupIndex|}}
            <tr>
                <th scope="row" colspan="3">{{group.name}}</td>
            </tr>

            {{#each staff as |s sIndex|}}
                <tr>
                    <th scope="row">&nbsp;</th>
                    <td><a href="/staff?staff_id={{s.id}}"></a>{{s.last_name}}, {{s.first_name}}</a></td>
                    <td class="text-center"><a href="/staff?staff_id={{s.id}}"><i class="fas fa-info-circle"></i></a></td>
                </tr>
            {{/each}}
        {{/each}}
    </tbody>
</table>